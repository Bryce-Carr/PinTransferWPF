<Window x:Class="PinTransferWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tools="clr-namespace:WindowTools"
        xmlns:local="clr-namespace:PinTransferWPF"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="Pin Transfer v2.0"
        Height="{Binding Source={x:Static SystemParameters.PrimaryScreenHeight}, Converter={tools:RatioConverter}, ConverterParameter='0.7' }" 
        Width="{Binding Source={x:Static SystemParameters.PrimaryScreenWidth}, Converter={tools:RatioConverter}, ConverterParameter='0.7' }" 
        WindowStartupLocation="CenterScreen"
        MinWidth="800"
        MinHeight="300"
        >
    
    <Window.Resources>
        <!-- Minimize Button Template -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <!-- Define the style for the minimize button here -->
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightGray" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="DarkGray" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <ControlTemplate x:Key="MinimizeButtonControlTemplate" TargetType="Button">
            <!-- Define the control template for the minimize button here -->
            <Border Background="{TemplateBinding Background}">
                <TextBlock Text="🗕" FontFamily ="Deja Vu" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </ControlTemplate>

        <!-- Maximize Button Template -->
        <ControlTemplate x:Key="MaximizeButtonControlTemplate" TargetType="Button">
            <Border Background="{TemplateBinding Background}">
                <Grid>
                    <TextBlock Text="🗖" FontFamily ="Deja Vu" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </ControlTemplate>

        <!-- Restore Button Template -->
        <ControlTemplate x:Key="RestoreButtonControlTemplate" TargetType="Button">
            <Border Background="{TemplateBinding Background}">
                <Grid>
                    <TextBlock Text="🗗" FontFamily ="Deja Vu" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </ControlTemplate>
        
        <!-- Close Button Template -->
        <ControlTemplate x:Key="CloseButtonControlTemplate" TargetType="Button">
            <!-- Define the control template for the close button here -->
            <Border Background="{TemplateBinding Background}">
                <TextBlock Text="✖" FontFamily ="Deja Vu" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
            </Border>
        </ControlTemplate>

        <!-- TitleBar ComboBox Template -->
        <ControlTemplate x:Key="TitleBarComboBoxTemplate" TargetType="{x:Type ComboBox}">
            <Grid>
                <ToggleButton x:Name="ToggleButton"
                          Grid.Column="0"
                          Margin="0,0,5,0"
                          Focusable="False"
                          ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                          Content="{TemplateBinding Tag}"
                          FontFamily="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(tools:ComboBoxExtensions.ButtonFontFamily)}"
                          FontSize="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(tools:ComboBoxExtensions.ButtonFontSize)}">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </ToggleButton.Template>
                    <ToggleButton.Style>
                        <Style TargetType="{x:Type ToggleButton}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="LightGray" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="DarkGray" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Background" Value="DarkGray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
                <Popup IsOpen="{Binding IsChecked, ElementName=ToggleButton}" 
                   Placement="Bottom"
                   AllowsTransparency="True" 
                   Focusable="False">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                        <ScrollViewer SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
            <ControlTemplate.Resources>
                <Style TargetType="{x:Type ComboBoxItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                <Border x:Name="Bd"
                                    Padding="5,3"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    SnapsToDevicePixels="true">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="true">
                                        <Setter TargetName="Bd" Property="Background" Value="#FFADADAD"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="#FF7E7E7E"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="false">
                                        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ControlTemplate.Resources>
        </ControlTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Rectangle Grid.Row="0" Grid.ColumnSpan="4" Fill="SlateGray"/>
        <DockPanel Grid.Column="3" HorizontalAlignment="Stretch"
           VerticalAlignment="Stretch"
           LastChildFill="True">

            <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Stretch"
                    DockPanel.Dock="Right"
                    HorizontalAlignment="Right">

                <Button x:Name="MinimizeButton"
                    KeyboardNavigation.IsTabStop="False"
                    Click="MinimizeWindow"
                    Style="{StaticResource TitleBarButton}"
                    Template="{StaticResource MinimizeButtonControlTemplate}"/>

                <Button x:Name="MaximizeButton"
                    KeyboardNavigation.IsTabStop="False"
                    Click="MaximizeClick"
                    Style="{DynamicResource TitleBarButton}"
                    Template="{DynamicResource MaximizeButtonControlTemplate}" />

                <Button x:Name="CloseButton"
                    KeyboardNavigation.IsTabStop="False"
                    Click="CloseWindow"
                    Style="{DynamicResource TitleBarButton}"
                    Template="{DynamicResource CloseButtonControlTemplate}"/>

            </StackPanel>
        </DockPanel>

        <DockPanel Grid.Column="0" Grid.ColumnSpan="4" Grid.RowSpan="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left"
                        HorizontalAlignment="Left">
                <Image Source="Assets/Mountain.ico" Height="30" Width="30"/>
                <ComboBox x:Name="fileComboBox" 
                        Template="{StaticResource TitleBarComboBoxTemplate}"
                        Tag="  File  "
                        tools:ComboBoxExtensions.ButtonFontFamily="Deja Vu"
                        tools:ComboBoxExtensions.ButtonFontSize="18"
                        SelectionChanged="fileComboBox_SelectionChanged">
                    <ComboBoxItem Content="Save Script"/>
                    <ComboBoxItem Content="Load Script"/>
                </ComboBox>

                <ComboBox x:Name="toolsComboBox" 
                        Template="{StaticResource TitleBarComboBoxTemplate}"
                        Tag="  Tools  "
                        tools:ComboBoxExtensions.ButtonFontFamily="Deja Vu"
                        tools:ComboBoxExtensions.ButtonFontSize="18"
                        SelectionChanged="toolsComboBox_SelectionChanged">
                    <ComboBoxItem Content="LabwareManager"/>
                </ComboBox>
            </StackPanel>       
        </DockPanel>

        <Viewbox x:Name="StackerGridViewbox" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="StackerGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <!-- Stacks will be added here programmatically -->
            </Grid>
        </Viewbox>
        <!-- Add other UI elements in the second row -->
        <!-- New UI elements for creating a run -->
        <Grid Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Margin="243,15,0,240" Width="311">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" >
                <Label Content="Run ID:" VerticalAlignment="Center" />
                <TextBox x:Name="txtRunID" HorizontalAlignment="Stretch" />
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" >
                <Label Content="Screen Number:" VerticalAlignment="Center" />
                <TextBox x:Name="txtScreenNumber" />
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" >
                <Label Content="User Name:" VerticalAlignment="Center" />
                <TextBox x:Name="txtUserName" />
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" >
                <Label Content="Current Script Line:" VerticalAlignment="Center" />
                <TextBox x:Name="txtCurrentScriptLine" />
            </StackPanel>
            <Button Grid.Row="1" x:Name="btnCreateRun" Margin="5" Padding="5" Content="Create Run" Click="btnCreateRun_Click" />
        </Grid>
        <Button x:Name="RunCommandsButton" Content="Run Commands" HorizontalAlignment="Left" Margin="643,101,0,0" Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" Click="RunCommandsButton_Click"/>
        <Button x:Name="CancelButton" Content="Pause" HorizontalAlignment="Left" Margin="648,143,0,0" Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" Click="CancelButton_Click" IsEnabled="False"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Name="StatusScrollViewer" Grid.Row="1" Grid.Column="1" Margin="-21,158,21,0" VerticalAlignment="Top" Height="240" Width="800">
            <TextBlock x:Name="StatusTextBlock" 
               TextWrapping="Wrap" 
               VerticalAlignment="Stretch" 
               HorizontalAlignment="Left"/>
        </ScrollViewer>
        <Button Content="ResumeButton" HorizontalAlignment="Left" Margin="692,143,0,0" Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" Click="ResumeButton_Click"/>
    </Grid>
</Window>